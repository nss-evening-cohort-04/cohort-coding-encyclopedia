namespace ArrayChecker
{
    /*

        Given an array with integers from 1 - 100 with one missing,
        make a class and method that can determine which number is missing.

        Given an array with integers from 1 - 100 with one number twice,
        make a method in your class that can determine which number is repeated.

        Given an array of numbers from 0 - 9 with any number of repeats, make a
        method to find out which number occurs in the array most often.

     */
}